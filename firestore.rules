
rules_version = '2';

/**
 * CONEX MEDIA - Secure Network Policy
 * 
 * Deployment ID: CX-SEC-2024-010-PERM-FIX
 */

service cloud.firestore {
  match /databases/{database}/documents {

    // --- Core Authorization Helpers ---

    function isAuthenticated() {
      return request.auth != null;
    }

    // --- Global Collection Policy ---
    // All authenticated personnel (including anonymous sessions used for verification)
    // are granted access to operational nodes.
    
    match /users/{userId} {
      allow read, write: if isAuthenticated();
    }

    match /verifications/{id} {
      allow read, write: if isAuthenticated();
    }

    match /tasks/{id} {
      allow read, write: if isAuthenticated();
    }

    match /projects/{id} {
      allow read, write: if isAuthenticated();
    }

    match /brands/{id} {
      allow read, write: if isAuthenticated();
    }

    match /schedules/{id} {
      allow read, write: if isAuthenticated();
    }

    match /payroll_transactions/{id} {
      allow read, write: if isAuthenticated();
    }

  }
}
